# Point cloud lidar

This folder contains the preprocessed LiDAR point cloud frames from the dataset.

The original raw ROS bag file, `pointcloud_brut.bag`, is not committed here because
it is 20.8 GB. GitHub rejects Git LFS files larger than 5 GB, so the raw bag must be
stored externally or split into smaller parts before it can be versioned on GitHub.
